const base = prompt('Digite a base do triângulo.');
const altura = prompt('Digite a altura do triângulo.')

const areaDoTriangulo =  (Number(base) * Number(altura)) / 2;

alert(`A área do triângulo é ${areaDoTriangulo}.`);