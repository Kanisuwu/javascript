const nome1 = 'Kanisu'; // String
const nome2 = "Kanisu"; // String
const nome3 = `Kanisu`; // String

const num1 = 10; // Number
const num2 = 10.5 // Number, EM outras linguagens é float

let nomeAluno; // Undefined = Não aponta para nenhum lugar na memória
const sobrenomeAluno = null; // Nulo -> Não aponta para a memória.
// Undefined != Null
// Null define valor nenhum. Undefined não foi aplicado um valor.
const aprovado = false; // Boolean = True ou False (Valor Lógico)

let a = 2;
const b = a;

console.log(a, b);

a = 3;

console.log(a, b);

// Todos os dados: String, Number, Undefined, Null, Boolean, Symbol