const names = ['Slugcat', 'Cat', 'Fox', 'Kanisu', 'Crunchycat'];

// splice(index, delete, add:elemento1, add:elementl2, add:elementl3);

const removed = names.splice(-1, 1)

console.log(names, removed);